# Rocket Swap 0.36.0

- The preset icon now fills the height of its fields instead of sitting at a fixed 32 px, so
  there is no longer an empty gap beside a preset that defines all four. The text still starts at
  the same place on every card, whatever size the icon is — cards that do not line up with each
  other stop reading as a list.
